#!groovy

properties([
    [
        $class: 'GithubProjectProperty',
        displayName: 'Divorce Case Progression Service',
        projectUrlStr: 'https://github.com/hmcts/div-case-progression-service'
    ],
    pipelineTriggers([
        [$class: 'GitHubPushTrigger']
    ])
])

@Library("Infrastructure@AddTestEnvironmentToTest")

def type = "java"
def product = "div"
def component = "case-progression"

def channel = '#div-dev'

static LinkedHashMap<String, Object> secret(String secretName, String envVar) {
    [ $class: 'AzureKeyVaultSecret',
      secretType: 'value',
      name: secretName,
      version: '',
      envVariable: envVar
    ]
}

List<LinkedHashMap<String, Object>> secrets = [
    secret("secret/test/ccidam/service-auth-provider/api/microservice-keys/divorceCcdSubmission", 'CASE_PROGRESSION_SERVICE_AUTH_SECRET'),
    secret("secret/test/divorce/draft/encryption_key", 'DRAFT_STORE_API_ENCRYPTION_KEY')
]

withPipeline(type , product, component) {
    before('functionalTest:aat') {
        env.TEST_ENVIRONMENT = "aat"
    }

//    after('functionalTest:aat') {
//        error('Halting pipeline before prod stages')
//    }

    after('checkout') {
        echo '${product}-${component} checked out'
    }

    loadVaultSecrets(secrets)

   // enableSlackNotifications(channel)
}
